%h3.text-center
  Search results for
  = "'#{@search_words.join(' ')}'"
%div.container
  %hr
  - if @search_results.size > 0
    %ul
      - @search_results.each do |result|
        %li
          = link_to result.name, [products_path, result.permalink].join('/'), :target => "_blank"
  - else
    %p
      Not found