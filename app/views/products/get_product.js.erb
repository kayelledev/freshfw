$('.editor-container-wr').html(
  "<%= j render(partial: 'shared/editor_container', locals: { object: root_product.has_variants? ? root_product.variants.first : root_product }) %>"
);
$(".editor-items-panel").html(
    "<%= j render(partial: 'shared/room_editor_panel_items', locals: { object: root_product.has_variants? ? root_product.variants.first : root_product }) %>"
);
