%table.data.orderItems
%thead
  %tr
    %th.name= 'Roles'
%tbody
  %tr.item{:class => 'existing'}
    %td.name= f.collection_select(:role_ids, Shoppe::Role.list_without_admin, :id, :name, {:selected => @permission.role_ids, include_hidden: false}, {:multiple => true})
