%h2= "Freight Company Name: #{@freight_company.name}"
- if @freight_company.zones.present?
  %h2 Freight Company Zones
  %table.data.orderItems
    %thead
      %tr
        %th= t('shoppe.logistics.zones.zone')
        %th= t('shoppe.logistics.zones.province')
        %th= t('shoppe.logistics.zones.country')
        %th= t('shoppe.logistics.zones.dest_city')
        %th= t('shoppe.logistics.links.action')
    %tbody
      - @freight_company.zones.each do |zone|
        - zone.cities.each do |city|
          %tr
            %td= zone.name
            %td= city.province
            %td= city.country.name
            %td= city.name
            %td= link_to t('shoppe.logistics.links.show'), zone
- else
  Please add zone for this freight company
         